1. На локальном репозитории сделать ветки для:
- Postman
- Jmeter
- CheckLists
- Bag Reports
- SQL
- Charles
- Mobile testing

2. Запушить все ветки на внешний репозиторий
3. В ветке Bag Reports сделать текстовый документ со структурой баг репорта
4. Запушить структуру багрепорта на внешний репозиторий
5. Вмержить ветку Bag Reports в Main
6. Запушить main на внешний репозиторий.
7. В ветке CheckLists набросать структуру чек листа.
8. Запушить структуру на внешний репозиторий
9. На внешнем репозитории сделать Pull Request ветки CheckLists в main
10. Синхронизировать Внешнюю и Локальную ветки Main

----------------------------------------------------------------------------

0. git branch
1. git checkout Postman && git checkout Jmeter && git checkout CheckLists && git checkout Bag 	Reports 	&& git checkout SQL && git checkout Charles && git checkout Mobile testing
2. git commit -am "add new branch" && git push
3. git checkout Bag Reports && git touch BugReport.txt (через vim добавляем структура баг репорта)
4. git commit -am "add file BugReport.txt" && git push
5. git checkout master && git merge Bag Reports
6. git commit -am "add branch: Bug Reports" && git push
7. git checkout CheckLists && git touch CheckList.txt (через vim добавляем структура баг репорта)
8. git commit -am "add new file CheckList.txt" && git push
9. <<На внешнем репозитории сделать Pull Request ветки CheckLists в main>>
10.git chtckout master && git pull